# Public (Vite) — safe to expose in the client bundle
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLIC_KEY=

# Anthropic: set only in Supabase Edge Function secrets (never VITE_*):
#   supabase secrets set ANTHROPIC_API_KEY=sk-ant-api03-...
# Used by: claude-feedback, skill-check-feedback
